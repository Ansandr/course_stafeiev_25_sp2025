# Компілятор мови програмування (Варіант 25)

Проєкт курсової роботи з дисципліни "Системне програмування". Реалізація компілятора для мови програмування варіанту 25.

---

## 📋 Вимоги

- **Visual Studio 2019+** (Platform Toolset v142 або новіший)
- **Windows 10/11**
- **Boost** (header-only, включено в проєкт) — <https://www.boost.org/>
- **Graphviz 14.0.5** (опціонально, для візуалізації автоматів) — <https://graphviz.org/download/>

---

## 📦 Опис проєктів

| Проєкт | Призначення |
| --- | --- |
| **`dfa_generator__2025`** | Генерує таблиці переходів DFA для лексичного аналізатора з регулярних виразів |
| **`dpda1_for_ll2_generator__2025`** | Створює документацію граматики LL(2) та автомата синтаксичного аналізатора |
| **`verify_syntax_by_EBNF__2025`** | Перевіряє коректність EBNF граматики (компактний варіант) |
| **`verify_syntax_by_EBNF_nonpacked__2025`** | Перевіряє коректність EBNF граматики через Boost.Spirit (детальний) |
| **`cw_sp2__2025_2026`** | Основний компілятор — лексичний, синтаксичний, семантичний аналіз та генерація коду |

---

## ⚠️ Важливо

**Генератори запускаються ПЕРЕД компіляцією основного компілятора!**

Генератори створюють файли в папках `built_src/` та `built_doc/`, які необхідні для роботи компілятора.

## 🚀 Швидкий старт

Відкрийте **`sp2025_var25.sln`** у Visual Studio

### Крок 1: Генератор лексичного аналізатора

**Проєкт:** `dfa_generator__2025`  
Зібрати та запустити

**Результат:** У папці `built_src/` з'являться файли `file1.hpp`, `file2.hpp`, `file3.hpp`, `file4.hpp`

### Крок 2: Генератор граматики

**Проєкт:** `dpda1_for_ll2_generator__2025`  
Зібрати та запустити

**Результат:** У папці `built_doc/` з'являться файли `Grammar_*.txt`, `NPDA_*.txt`

### Крок 3 (опціонально): Перевірка граматики

**Проєкт:** `verify_syntax_by_EBNF__2025` (компактний) або `verify_syntax_by_EBNF_nonpacked__2025` (з Boost.Spirit)  
Зібрати та запустити

### Крок 4: Компілятор

**Проєкт:** `cw_sp2__2025_2026`  
**Дія:** Зібрати  
**Аргументи запуску:** `..\base_test_programs_2025\file0.k03`  
**Де налаштувати:** Project Properties → Debugging → Command Arguments

---

## 📝 Специфікація мови (Варіант 25)

### Структура програми

```k03
program AB0;
start var int32 AC1, AD2;
  read AC1;
  AC1 add 10 :> AD2;
  write AD2;
end
```

### Ідентифікатори

**Формат:** `[A-Z][A-Z][0-9]` — рівно **2 великі літери** + **1 цифра**

✅ Правильно: `AA0`, `XY9`, `AB1`  
❌ Неправильно: `A0`, `ABC1`, `aa0`

### Ключові слова

`program` `start` `end` `var` `int32` `read` `write` `if` `else` `while` `break` `continue`
🔧 Адаптація під свій варіант

Для створення компілятора під **власний варіант** мови:

21. **Використовуйте заготовки** з репозиторію:  
   🔗 <https://github.com/KozakNazar/cw_sp2__2024_2025/tree/main/EBNFVerify%2BGrammarVerify>

2. Відредагуйте файли у папці **`variant_specification/`**:
   - `LexicaByRegExAndSyntaxByLL2protototype.h` — токени та регулярні вирази
   - `EBNF_N1.h` — граматика мови

3. Після змін **повторіть кроки 1-4** (генератори + компіляція)

---

## 🐛 Налагодження

### Увімкнення трасування синтаксичного аналізатора

**Файл:** `cw_sp2__2025_2026/src/include/syntax/syntax.h`

Розкоментуйте рядок 48:

```cpp
#define DEBUG_STATES
```

### Увімкнення трасування лексичного аналізатора

**Файл:** `built_src/dfa.hpp`

Розкоментуйте рядок 7:

```cpp
#define DEBUG_DFA_STATE
```

Після змін **перекомпілюйте** проєкт `cw_sp2__2025_2026` (Крок 4️⃣)

---

### Ключові слова (lowercase)

`program`, `start`, `end`, `var`, `int32`, `read`, `write`, `if`, `else`, `while`, `break`, `continue`

### Оператори

- **Арифметичні:** `add`, `-`, `mul`, `/`, `%`
- **Порівняння:** `==`, `!=`, `lt` (less than), `gt` (greater than)
- **Логічні:** `not`, `and`, `or`
- **Присвоєння:** `:>` (зліва направо, наприклад: `вираз :> змінна`)
- **Індексація:** `[]`
- **Блоки:** `{}`, `()`

### Коментарі

- Початок: `/*`
- Завершення: `*/` або кінець рядка
- *❗ Типові помилки
